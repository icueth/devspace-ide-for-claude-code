node_modules
dist-electron
out
release
.DS_Store
*.log
.vite
.eslintcache
.playwright-mcp
.claude
.devspace

# Ruflo per-project init artifacts (npx ruflo init) — local state + the
# ruflo-generated config/MCP/CLAUDE.md, not part of this product's source.
/.claude-flow/
/.mcp.json
/CLAUDE.md
# Ruflo swarm/memory runtime state, written to the project root on first
# `ruflo swarm`/`agent`/`memory`/`session` use.
/.swarm/
/ruvector.db
/agentdb.rvf
/agentdb.rvf.lock

# Local tooling — mempalace mining, scratch mockups
mempalace.yaml
entities.json
mockup/

# Bundled uv binaries (fetched at build time via scripts/fetch-uv.mjs)
resources/mempalace-uv/*/uv
resources/mempalace-uv/*/uv.exe